import Loading from "@/app/[locale]/loading";

const LoadingTemplateSlot = () => (
  <div className="w-full h-full bg-gr-multi-dark sx:rounded-t-xl sm:rounded-r-xl">
    <Loading />
  </div>
);

export default LoadingTemplateSlot;
